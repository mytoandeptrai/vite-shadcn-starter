---
phase: 01-pencil-base-design-auth-profile-landing
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routes/(auth)/sign-in.tsx
  - src/routes/(auth)/sign-up.tsx
  - src/routes/(auth)/forgot-password.tsx
  - src/routes/(auth)/reset-password.tsx
  - src/routes/(auth)/success-notification.tsx
  - src/routes/(private)/profile.tsx
  - src/constant/route.const.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - PROF-01
  - LAND-02
user_setup: []
must_haves:
  truths:
    - "Auth routes exist for sign-in/sign-up/forgot/reset/success and render non-placeholder UI skeletons"
    - "Profile route exists under private area and renders a profile skeleton consistent with PageContainer pattern"
  artifacts:
    - path: "src/routes/(auth)/sign-in.tsx"
      provides: "Sign-in route page skeleton"
      min_lines: 15
    - path: "src/routes/(private)/profile.tsx"
      provides: "Profile route page skeleton"
      min_lines: 15
  key_links:
    - from: "src/routes/(private)/layout.tsx"
      to: "src/routes/(private)/profile.tsx"
      via: "navigation route constant"
      pattern: "ROUTES\\.PROFILE"
---

<objective>
Nâng các route auth/profile từ placeholder “Hello …” thành skeleton UI cơ bản (để bám theo design Pencil Phase 1), và đảm bảo route constants có đủ đường dẫn cần dùng.

Purpose: Chuẩn bị nền code để Phase 2 triển khai auth/profile thật nhanh, không bị thiếu route/skeleton.
Output: Auth routes + profile route có UI skeleton (không còn “Hello …”).
</objective>

<execution_context>
@/Users/macbookpro/Desktop/Projects/vite-shadcn-starter/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/macbookpro/Desktop/Projects/vite-shadcn-starter/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-pencil-base-design-auth-profile-landing/01-CONTEXT.md

@src/routes/(auth)/layout.tsx
@src/components/containers/page-container.tsx
@src/styles.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thay placeholder auth pages bằng layout skeleton</name>
  <files>
    src/routes/(auth)/sign-in.tsx,
    src/routes/(auth)/sign-up.tsx,
    src/routes/(auth)/forgot-password.tsx,
    src/routes/(auth)/reset-password.tsx,
    src/routes/(auth)/success-notification.tsx
  </files>
  <read_first>
    src/routes/(auth)/layout.tsx,
    src/components/ui/card.tsx,
    src/components/ui/button.tsx,
    src/components/ui/input.tsx,
    src/styles.css
  </read_first>
  <action>
  Trong mỗi route auth:
  - Render 1 card centered (tận dụng layout `(auth)` đã center sẵn).
  - Nội dung skeleton tối thiểu:
    - title + subtitle
    - form fields (email/password…) tuỳ màn
    - primary button
    - links điều hướng giữa sign-in / sign-up / forgot password
  - Dùng components sẵn có từ `src/components/ui/*` (Card/Button/Input).
  - Không cần logic API thật trong Phase 1 (chỉ UI skeleton).
  </action>
  <verify>pnpm -s check (hoặc pnpm -s build) chạy pass</verify>
  <acceptance_criteria>
    - src/routes/(auth)/sign-in.tsx does not contain "Hello"
    - src/routes/(auth)/sign-up.tsx does not contain "Hello"
    - src/routes/(auth)/forgot-password.tsx does not contain "Hello"
    - src/routes/(auth)/reset-password.tsx does not contain "Hello"
    - src/routes/(auth)/success-notification.tsx does not contain "Hello"
  </acceptance_criteria>
  <done>Các auth routes hiển thị UI skeleton thay vì placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Chuẩn hoá profile route skeleton (private)</name>
  <files>src/routes/(private)/profile.tsx</files>
  <read_first>src/routes/(private)/layout.tsx, src/components/containers/page-container.tsx, src/components/ui/user-avatar-profile.tsx</read_first>
  <action>
  Cập nhật `src/routes/(private)/profile.tsx`:
  - Render bằng `PageContainer` (pageTitle/pageDescription) để đúng pattern dashboard.
  - Hiển thị skeleton profile (avatar + name/email placeholders + nút Edit).
  - Không cần fetch user thật trong Phase 1 (AuthProvider hiện vẫn stub).
  </action>
  <verify>pnpm -s check (hoặc pnpm -s build) chạy pass</verify>
  <acceptance_criteria>
    - src/routes/(private)/profile.tsx contains "PageContainer"
    - src/routes/(private)/profile.tsx does not contain "Hello"
  </acceptance_criteria>
  <done>Profile route có skeleton đúng layout private.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm -s check` pass
- [ ] Auth routes + profile route không còn placeholder “Hello …”
</verification>

<success_criteria>
- Auth pages có UI skeleton để bám design Phase 1
- Profile page có UI skeleton theo PageContainer
</success_criteria>

<output>
After completion, create `.planning/phases/01-pencil-base-design-auth-profile-landing/01-02-SUMMARY.md`
</output>

